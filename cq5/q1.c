#include<stdio.h>
int main()
{
int length,width,area;
printf("enter the values of length and width of a rectangle\n");
scanf("%d%d",&length,&width);
area=length*width;
printf("area of the rectangle is :%d\n",area);
}
